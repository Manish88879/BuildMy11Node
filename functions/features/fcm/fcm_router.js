const express = require('express');
const router = express.Router();

const fcmController = require('./fcm_controller');

// router.post("/sendNotifications", fcmController.fcmAPI);

module.exports = router;